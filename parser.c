#include "main.h"

/**
 * parse - parse any special characters in the cmdv
 * @box: container
 *
 * Return: 0 (SUCCESS) | (FAIL)
 */
int parse(container_t *box)
{
	container_t *unused1 __attribute__((unused)) = box;

	/* parse here */
	return (0);
}
