#include "main.h"

/**
 * parse - parse any special characters in the cmdv
 * @cmdv: array of commands
 * @cmdc: number of commands
 *
 * Return: 0 (SUCCESS) | (FAIL)
 */
int parse(char **cmdv, size_t *cmdc)
{
	char **unused1 __attribute__((unused)) = cmdv;
	size_t *unused2 __attribute__((unused)) = cmdc;

	/* parse here */
	return (0);
}
